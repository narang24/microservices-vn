apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: dev-ns
spec:
  type: {{.Values.service.type}}
  selector:
    app: auth-service
  ports:
    - port: {{.Values.service.port}}
      targetPort: {{.Values.containerPort}}